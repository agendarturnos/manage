# Manage App

Este repositorio contiene un mockup inicial en `index.html` y una versión funcional basada en Vite y Firestore dentro de la carpeta `app`.

Para comenzar a trabajar con la versión en Vite revisa el archivo [`app/README.md`](app/README.md).
